# Webis Platform v2.0 架构演进路线图 (Roadmap)

本文档详细规划了将 Webis 从单一工具脚本转型为**综合性 LLM 基础设施平台**的设计路径。

## 🌟 愿景 (Vision)
打造一个**模块化、可扩展、具备记忆能力**的数据智能平台。
- **前侧 (Ingestion)**: 开放生态，支持任意数据源插件化接入。
- **中间 (Kernel)**: 深度清洗、结构化、向量化，构建高质量“知识湖仓”。
- **后侧 (Application)**: 基于数据资产，快速构建 RAG、研报、监控等上层应用。
## 🎯 项目定位与差异化 (Positioning)
明确 Webis 在开源生态中的独特价值主张 (Value Proposition)。
- [ ] **竞品分析文档**: 对比 LlamaIndex, LangChain, Unstructured, Firecrawl 等项目，明确 Webis 的差异化优势。
- [ ] **核心 Slogan**: 一句话说清项目价值 (如 "From Web to Wisdom: Your AI-Powered Knowledge Pipeline")。
- [ ] **Target Persona**: 明确目标用户画像 (数据工程师? 研究员? 企业情报分析师?)。
---

## 📅 第一阶段：基石 - 标准化与解耦 (Foundation)
**目标**: 建立类型安全的核心协议，实现模块的插件化，摆脱硬编码。

### 1.1 核心数据模型设计 (`core/schema.py`)
建立全系统通用的数据流转标准，使用 `Pydantic` 强类型约束。
- [x] **定义 `WebisDocument`**: 系统内流转的最小单元。
    - `id`: UUID
    - `content`: 原始内容 (str)
    - `meta`: 元数据 (url, timestamp, source_plugin, author...)
    - `type`: 数据类型 (html, pdf, image, text)
- [x] **定义 `StructuredResult`**: 结构化输出标准。
    - `schema_id`: 对应哪个抽取模板
    - `data`: 提取出的 JSON 数据
    - `lineage`: 溯源信息 (source_doc_id, prompt_version, model_name)
- [x] **定义 `PipelineContext`**: 贯穿任务生命周期的上下文对象 (config, logger, run_id)。

### 1.2 插件系统架构 (`core/plugin/`)
设计“一切皆插件”的注册机制。
- [x] **定义 `SourcePlugin` 接口**:
    - `fetch(query, config) -> Iterator[WebisDocument]`
    - 示例实现: `BaiduSearchPlugin`, `LocalFilePlugin`, `FirecrawlPlugin`
- [x] **定义 `ProcessorPlugin` 接口**:
    - `process(doc: WebisDocument) -> WebisDocument`
    - 示例实现: `HtmlCleanerPlugin`, `PdfOcrPlugin`, `PiiRedactorPlugin` (隐私清洗)
- [x] **插件注册表 (Registry)**:
    - 实现动态加载机制，支持通过配置字符串加载类 (e.g., `"plugins.sources.BaiduSearchPlugin"`).

### 1.3 核心重构
- [x] **重写 `webis_pipeline.py`**:
    - 不再直接 import 具体工具。
    - 改为读取 `config.yaml`，根据配置动态组装 Pipeline (DAG)。
- [x] **迁移旧版爬虫**: 将 `crawler/` 下的 SerpApi, Baidu, DDG, GitHub, HN 等工具迁移为标准 `SourcePlugin`。
- [x] **配置管理**: 引入 `Hydra` 或 `Dynaconf` 统一管理多环境配置 (已实现基础 `ConfigManager`).

### 1.4 Pipeline 高级特性 (`core/pipeline/`)
设计灵活、健壮的数据流水线引擎。
- [x] **DAG 执行引擎**: 支持有向无环图定义任务依赖，而非线性流程。
- [x] **条件分支**: 根据数据类型或内容动态选择处理路径 (如: PDF -> OCR分支, HTML -> 直接清洗)。
- [x] **错误重试策略**: 
    - 可配置重试次数、指数退避 (Exponential Backoff)。
    - 支持部分失败继续 (Partial Failure Tolerance)。
- [x] **断点续传 (Checkpoint)**: 长任务中断后可从上次进度恢复。
- [x] **流式处理 (Streaming)**: 支持边抓取边处理，降低内存占用，提升首字节响应速度。
- [x] **批量优化 (Batching)**: 自动合并小请求为批量 LLM 调用，降低 API 开销。
- [x] **超时控制**: 每个 Plugin 可配置独立超时时间。

### 1.5 LLM 层抽象 (`core/llm/`)
统一大模型调用逻辑，支持多模型灵活切换。
- [x] **Model Router**: 
    - 根据任务类型自动选择模型 (简单任务用小模型，复杂任务用大模型)。
    - 支持 Fallback 链 (主模型失败自动切换备用模型)。
- [x] **成本控制**:
    - Token 计数与预算限制。
    - 按任务/用户/时间段统计消耗。
- [x] **响应缓存**: 相同输入复用历史结果，避免重复调用 (基于内容哈希)。
- [x] **Prompt 版本管理**: 
    - Prompt 模板存储在数据库/文件系统。
    - 支持 A/B 测试不同 Prompt 版本效果。
- [x] **多模型适配器**: 统一接口支持 OpenAI / Claude / DeepSeek / 本地模型 (Ollama)。

---

## 🧠 第二阶段：记忆 - 持久化与知识湖仓 (Memory)
**目标**: 数据不再流失，沉淀为可检索、可复用的资产。

### 2.1 关系型存储 (Metadata Store)
- [x] **数据库选型**: 开发环境 SQLite，生产环境 PostgreSQL。
- [x] **ORM 层**: 使用 `SQLAlchemy` 或 `SQLModel`。
- [x] **存储逻辑**:
    - `documents` 表: 存储原始抓取内容 (S3/MinIO 路径或 Text)。
    - `extractions` 表: 存储 LLM 结构化后的 JSON 数据 (集成在 DocumentModel).
    - `runs` 表: 记录每次任务的执行状态、耗时、Token 消耗。

### 2.2 向量化存储 (Semantic Store)
- [x] **向量库集成**: 引入 `ChromaDB` (轻量) 或 `Milvus` (生产)。
- [x] **Embedding 流水线**:
    - [x] 实现 `ChunkingPlugin`: 文本切片 (RecursiveCharacterTextSplitter)。
    - [x] 实现 `EmbeddingPlugin`: 调用 OpenAI/HuggingFace 模型生成向量。
- [x] **混合检索**: 封装检索接口，支持 `Vector Search` + `Metadata Filter`。

### 2.3 数据治理
- [x] **去重机制**: 基于 URL 或 内容哈希 (MD5/SimHash) 避免重复处理。
- [ ] **版本控制**: 数据的“快照”机制，支持回滚到某次清洗前的状态。
### 2.4 多模态数据支持 (`plugins/multimodal/`)
扩展系统处理非文本数据的能力。
- [x] **图像处理**:
    - OCR 文字提取 (Tesseract / PaddleOCR / 云服务)。
    - 图像描述生成 (GPT-4V / Qwen-VL)。
    - 图表数据提取 (表格、柱状图 -> 结构化数据)。
- [x] **音频处理**:
    - 语音转文字 (Whisper).
    - 会议/播客自动摘要.
- [x] **视频处理**:
    - 关键帧提取 + 描述。
    - 字幕提取与翻译。

### 2.5 增量与实时数据 (`core/incremental/`)
支持持续数据更新场景。
- [x] **增量抓取**:
    - 记录上次抓取时间/游标，只获取新数据。
    - 基于 RSS/Atom Feed 的增量订阅。
- [x] **变更检测 (Change Detection)**: 定期检查页面变化，仅重新处理变更部分。
- [x] **实时流接入**: 支持 Kafka / Redis Stream 作为数据源，实现近实时处理。
---

## 🚀 第三阶段：服务 - API 化与应用生态 (Service)
**目标**: 开放能力，让外部系统通过 API 调用 Webis。

### 3.1 API 网关 (`server/`)
- [x] **FastAPI 服务化**:
    - `POST /api/v1/ingest`: 提交抓取任务 (异步)。
    - `GET /api/v1/tasks/{id}`: 查询任务进度。
    - `POST /api/v1/query`: RAG 问答接口。
    - `GET /api/v1/knowledge`: 结构化数据查询接口 (暂未实现).
- [x] **任务队列**: 引入 `Celery` + `Redis`，支持大规模并发抓取和耗时清洗任务。

### 3.2 应用 SDK (`sdk/`)
- [x] **Python Client**: `pip install webis-sdk`
    ```python
    client = WebisClient(api_key="...")
    docs = client.search("新能源汽车", mode="hybrid")
    report = client.generate_report(docs, template="industry_analysis")
    ```

### 3.3 典型应用场景 (Applets)
- [x] **Webis Monitor (监控)**: 定时运行 Pipeline，发现特定关键词 (如"裁员", "违约") 自动推送到 Slack/钉钉。
- [x] **Webis Chat (问答)**: 基于沉淀的知识库，提供准确的行业问答。
- [x] **Webis Visualizer Pro**: 升级现有的可视化，支持动态图表 (ECharts) 和交互式仪表盘。

### 3.4 质量保障体系 (`core/quality/`)
确保数据和结果的可靠性。
- [x] **数据质量评分 (Data Quality Score)**:
    - 完整性 (字段缺失率)。
    - 准确性 (与已知真值对比)。
    - 时效性 (数据新鲜度)。
- [x] **结构化验证器 (Schema Validator)**:
    - 基于 JSON Schema / Pydantic 验证 LLM 输出。
    - 自动修复常见格式错误 (如日期格式不统一)。
- [x] **置信度标注**: LLM 输出附带置信度分数，低置信度结果标记人工审核。
- [x] **人机协作 (Human-in-the-Loop)**:
    - 低置信度结果进入审核队列。
    - 人工修正反馈用于 Prompt 优化。

### 3.5 开发者工具 (`tools/dev/`)
提升开发和调试效率。
- [x] **Playground Web UI**:
    - 可视化 Pipeline 编排 (拖拽式)。
    - 实时预览每个节点的输入/输出。
    - Prompt 调试沙盒。
- [x] **数据浏览器 (Data Explorer)**:
    - 浏览知识库中的文档和结构化数据。
    - 支持全文搜索和语义搜索。
- [x] **Pipeline Profiler**: 分析每个阶段的耗时和资源消耗，识别瓶颈。
- [x] **Mock Mode**: 使用预设数据替代真实抓取，加速开发迭代。

---

## 🔮 第四阶段：智能 - 高级特性 (Intelligence)
**目标**: 提升系统的自动化程度和推理能力。

### 4.1 Graph RAG (图谱增强)
- [x] **图数据库**: 集成 `Neo4j`。
- [x] **关系抽取**: 升级 Structuring Agent，专门提取 `(Entity) -> [Relation] -> (Entity)` 三元组。
- [x] **图谱问答**: 利用图谱路径进行多跳推理 (Multi-hop Reasoning)。

### 4.2 Agentic Workflow (智能体工作流)
- [x] **自我修正**: 如果抓取失败或清洗结果为空，Agent 自动尝试更换 User-Agent、更换代理或更换解析策略。
- [x] **主动规划**: 用户输入模糊目标 ("分析苹果公司现状")，Agent 自动拆解为 ("抓取股价", "抓取新闻", "抓取财报") 多个子任务。

### 4.3 高级结构化能力 (`core/extraction/`)
提升信息提取的深度和精度。
- [x] **Schema 自动推断**: 根据样本数据自动生成 JSON Schema，减少人工定义。
- [x] **Few-shot 学习**: 支持用户提供少量标注样本，快速适配新领域。
- [x] **跨文档融合**: 从多个文档中提取同一实体的信息并合并去重。
- [x] **时序事件提取**: 识别事件的时间顺序，构建时间线。
- [x] **情感/立场分析**: 对新闻或评论进行情感极性和立场倾向判断。

### 4.4 可扩展性架构 (`core/distributed/`)
支持大规模生产环境部署。
- [x] **分布式执行**: 
    - Worker 节点水平扩展。
    - 任务分片与负载均衡。
- [x] **多租户支持**: 数据隔离，按租户配置 Pipeline 和权限。
- [x] **弹性伸缩**: 基于队列深度自动扩缩 Worker 数量 (K8s HPA)。
- [x] **跨区域部署**: 支持边缘节点就近抓取，中心节点统一处理。

---

## 🛠 基础设施与工程化 (Infra)
- [x] **Docker 化**: 提供 `docker-compose.yml` 一键启动 (API + DB + Redis + Chroma + Neo4j)。
- [x] **可观测性**: 集成 `LangSmith` 或 `OpenTelemetry` 监控 LLM 调用链路和成本。
- [x] **测试体系**: 建立 `tests/` 目录，覆盖核心 Plugin 的单元测试。
- [x] **性能基准 (Benchmark)**: 标准数据集上的处理速度、准确率对比测试。
- [x] **负载测试**: 使用 Locust/k6 模拟高并发场景，识别系统瓶颈。

---

## 🧩 插件生态规范 (Plugin Ecosystem)
**目标**: 建立标准化的插件开发、分发和管理机制。

### 插件开发规范
- [x] **Plugin Template**: 提供 `cookiecutter` 模板，一键生成插件项目骨架。
- [x] **Plugin SDK**: 封装常用工具函数 (HTTP 请求、解析、日志)。
- [x] **测试脚手架**: 插件模板内置测试用例框架。
- [x] **文档规范**: 每个插件必须包含 README、示例、配置说明。

### 插件分发机制
- [x] **官方插件仓库**: `webis-plugins` GitHub Org，托管官方认证插件。
- [x] **社区插件索引**: `awesome-webis-plugins` 列表收录第三方插件。
- [x] **插件市场 (Plugin Hub)**: Web UI 浏览、搜索、一键安装插件。
- [x] **版本兼容矩阵**: 明确每个插件支持的 Webis Core 版本。

### 插件质量保障
- [ ] **自动化测试**: 插件提交 PR 时自动运行兼容性测试。
- [ ] **安全扫描**: 检测插件代码中的敏感操作 (如任意代码执行)。
- [ ] **评分系统**: 基于下载量、Issue 响应速度、测试覆盖率的插件评分。

---

## 🌍 国际化与社区建设 (Internationalization & Community)
**目标**: 建立健康的开源社区，吸引全球贡献者。

### 5.1 文档国际化 (i18n)
- [x] **英文优先**: README, API 文档, 代码注释统一使用英文，降低国际开发者门槛。
- [x] **多语言 README**: 提供 `README.md` (EN), `README_CN.md` (中文), `README_JA.md` (日文) 等。
- [x] **文档站点**: 使用 `MkDocs` / `Docusaurus` 搭建独立文档网站，支持版本切换和搜索。
- [ ] **API Reference**: 自动从代码注释生成 API 文档 (Sphinx / pdoc)。

### 5.2 贡献者体验 (Contributor Experience)
- [x] **CONTRIBUTING.md**: 编写贡献指南 (如何提 Issue, 如何提 PR, 代码风格规范)。
- [x] **CODE_OF_CONDUCT.md**: 采用 Contributor Covenant 行为准则。
- [x] **Issue 模板**: 提供 Bug Report / Feature Request / Plugin Request 等模板。
- [x] **PR 模板**: 标准化 Pull Request 描述格式。
- [ ] **Good First Issues**: 标记适合新人入门的简单任务。
- [ ] **开发者文档**: 详细描述架构、模块职责、如何开发插件。

### 5.3 社区运营
- [ ] **Discord / Slack 频道**: 建立实时交流社区。
- [ ] **GitHub Discussions**: 启用讨论区，用于 Q&A 和 RFC 讨论。
- [ ] **定期 Office Hours**: 核心维护者每周/月定时答疑 (YouTube/Bilibili 直播)。
- [ ] **Awesome Webis 列表**: 收录社区插件、案例、教程。

---

## 🧑‍💻 开发者体验 (Developer Experience - DX)
**目标**: 让开发者能在 5 分钟内跑通第一个示例。

### 6.1 快速上手 (Quick Start)
- [ ] **一键安装**: `pip install webis` 或 `uv pip install webis`。
- [x] **Minimal Example**: 3 行代码完成"抓取 -> 清洗 -> 结构化" (见 `examples/basic_pipeline.py`)。
    ```python
    from webis import Pipeline
    pipe = Pipeline.from_preset("news_analyst")
    result = pipe.run("最近的AI新闻")
    ```
- [ ] **Colab/Jupyter Notebook**: 提供可直接运行的云端示例。

### 6.2 CLI 工具 (`webis` command)
- [x] **命令行接口**: 
    - `webis init` - 初始化项目/配置文件。
    - `webis run "任务描述"` - 执行端到端 Pipeline。
    - `webis plugins list` - 列出已安装插件。
    - `webis plugins install <name>` - 安装社区插件。
    - `webis serve` - 启动 API 服务。
- [ ] **交互模式**: `webis chat` 进入对话式数据探索。

### 6.3 错误处理与调试
- [x] **友好的错误信息**: 错误提示包含修复建议 (类似 Rust 编译器风格)。
- [x] **Debug 模式**: `--debug` 标志输出详细的中间过程 (每个 Plugin 的输入/输出)。
- [x] **Dry Run**: `--dry-run` 模拟执行，不实际调用 LLM，用于调试 Pipeline 配置。

---

## 🔒 安全与合规 (Security & Compliance)
**目标**: 企业级安全标准，保护用户数据。

### 7.1 安全机制
- [x] **Secret Management**: 敏感信息 (API Keys) 不硬编码，支持 `.env`, Vault, AWS Secrets Manager。
- [x] **PII Redactor Plugin**: 内置隐私数据脱敏插件 (电话、身份证、邮箱等)。
- [x] **沙箱执行**: 爬虫代码在受限环境中执行，防止恶意代码注入。
- [x] **依赖审计**: 定期运行 `pip-audit` / `safety` 检查依赖漏洞。

### 7.2 合规支持
- [x] **GDPR 友好**: 提供数据删除接口 (`DELETE /api/v1/documents/{id}`)。
- [x] **数据保留策略**: 可配置自动过期删除 (如 30 天后清理原始数据)。
- [x] **审计日志**: 记录所有数据访问和修改操作。

---

## 📦 发布与版本管理 (Release & Versioning)
**目标**: 专业的版本发布流程，保障用户升级体验。

### 8.1 版本策略
- [x] **语义化版本 (SemVer)**: `MAJOR.MINOR.PATCH`，明确 Breaking Change 规则。
- [x] **Changelog 自动化**: 使用 `Conventional Commits` + `release-please` 自动生成 CHANGELOG。
- [x] **向后兼容策略**: 核心 API 变更需提前一个版本发出 Deprecation Warning。

### 8.2 CI/CD 流水线
- [x] **GitHub Actions**:
    - `lint.yml`: 代码风格检查 (Ruff, Black, isort)。
    - `test.yml`: 单元测试 + 集成测试 (pytest)。
    - `release.yml`: Tag 触发自动发布到 PyPI。
    - `docs.yml`: 文档自动构建部署到 GitHub Pages。
- [x] **Pre-commit Hooks**: 强制本地提交前通过 lint 和格式化。
- [x] **覆盖率报告**: 集成 Codecov，PR 时显示覆盖率变化。

### 8.3 发布物
- [x] **PyPI 包**: `pip install webis` (pyproject.toml 已配置)。
- [ ] **Docker Image**: `docker pull webis/webis:latest`。
- [x] **Homebrew Formula**: `brew install webis` (macOS 用户友好)。

---

## 📊 增长与运营 (Growth & Operations)
**目标**: 提升项目知名度和用户采纳率。

### 9.1 品牌建设
- [ ] **Logo & 视觉规范**: 设计专业的项目 Logo 和配色方案。
- [ ] **官方网站**: 独立落地页 (`webis.dev`)，展示功能、案例、文档入口。
- [ ] **Demo 视频**: 60 秒快速演示核心价值。

### 9.2 内容运营
- [ ] **技术博客**: 发布架构解析、最佳实践、版本更新文章。
- [ ] **Case Studies**: 展示真实用户案例 (如"某公司如何用 Webis 构建情报系统")。
- [ ] **教程系列**: YouTube / Bilibili 发布渐进式视频教程。

### 9.3 社区指标 (Metrics)
- [ ] **Star History**: 跟踪 GitHub Star 增长趋势。
- [ ] **下载量**: PyPI 下载统计 (pepy.tech)。
- [ ] **Issue 响应时间**: 目标 < 48 小时首次响应。
- [ ] **贡献者数量**: 目标第一年达到 20+ 贡献者。

---

## 🏛 项目治理 (Governance)
**目标**: 建立透明、可持续的项目治理结构。

### 10.1 决策流程
- [ ] **RFC 机制**: 重大功能变更需撰写 RFC (Request for Comments)，社区讨论后实施。
- [ ] **Roadmap 透明化**: 每季度公开更新 Roadmap，GitHub Project Board 可视化进度。
- [ ] **决策记录 (ADR)**: Architecture Decision Records 记录重要技术决策及其理由。

### 10.2 维护者结构
- [x] **Core Maintainers**: 核心维护者团队 (写入 MAINTAINERS.md)。
- [x] **Plugin Maintainers**: 各官方插件的独立维护者。
- [ ] **Triagers**: 负责 Issue 分类和初步响应的志愿者。

### 10.3 可持续性
- [ ] **Sponsor 机制**: GitHub Sponsors / Open Collective 接受捐赠。
- [ ] **企业支持**: 考虑提供企业版支持服务 (SLA, 私有化部署咨询)。
